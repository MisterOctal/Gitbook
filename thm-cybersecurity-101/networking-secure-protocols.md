---
icon: chart-network
cover: >-
  https://tryhackme-images.s3.amazonaws.com/user-uploads/5f04259cf9bf5b57aed2c476/room-content/5f04259cf9bf5b57aed2c476-1721904048318.png
coverY: 0
coverHeight: 138
---

# Networking Secure Protocols

**Date:** 22.03.2026

**Room Category:** Walkthrough

**Core Concept:** Transitioning from plaintext to encrypted communications using TLS, SSH, and VPNs to ensure confidentiality, integrity, and authenticity.

In the previous room, we saw how easy it is to intercept passwords and data when using "Core" protocols like HTTP, FTP, and Telnet. This room explores the "Secure" versions of those protocols. By adding a layer of encryption, we protect sensitive information from eavesdroppers and ensure that the server we are talking to is legitimate. Mastering these secure implementations is the standard for modern systems administration and security auditing.

***

## Task 1: Introduction

The core issue with early internet protocols is that they were designed for functionality, not security. Without encryption, data has no **Confidentiality** (anyone can read it), **Integrity** (anyone can change it), or **Authenticity** (anyone can pretend to be the server). We use Transport Layer Security (TLS) and Secure Shell (SSH) to solve these problems.

Question: Please ensure you have finished the Networking Core Protocols room at minimum.

> **Answer:** No answer needed

***

## Task 2: TLS

TLS is the successor to SSL (Secure Sockets Layer). It provides the encryption "wrapper" for other protocols. To verify a server's identity, TLS uses **Certificates** issued by a **Certificate Authority (CA)**.

* **Trusted Certificates:** Validated by a third-party CA (e.g., Let's Encrypt).
* **Self-Signed Certificates:** Generated by the server itself. These provide encryption but **do not** guarantee authenticity, as anyone can create one.

<figure><img src="../.gitbook/assets/image (75).png" alt="" width="563"><figcaption></figcaption></figure>

Question: What is the protocol name that TLS upgraded and built upon?

> **Answer:** SSL

Question: Which type of certificates should not be used to confirm the authenticity of a server?

> **Answer:** self-signed certificate

***

## Task 3: HTTPS

HTTPS is simply HTTP running over a TLS tunnel. While a standard HTTP connection only requires a TCP 3-way handshake, HTTPS requires an additional **TLS Handshake** to negotiate encryption keys before any web data is sent.

**Key Visual Identifiers in Wireshark:**

* **Port 80:** Plaintext HTTP (readable).
* **Port 443:** Encrypted HTTPS (appears as "Application Data").

Question: How many packets did the TLS negotiation and establishment take in the Wireshark HTTPS screenshots above?

> **Answer:** 8

Question: What is the number of the packet that contains the GET /login when accessing the website over HTTPS?

> **Answer:** 10

***

## Task 4: SMTPS, POP3S, and IMAPS

Just like web traffic, email protocols have secure versions that utilize TLS to prevent attackers from reading private correspondence or stealing login credentials.

| Protocol | Plaintext Port | Secure (TLS) Port |
| -------- | -------------- | ----------------- |
| **SMTP** | 25             | 465 / 587         |
| **POP3** | 110            | 995               |
| **IMAP** | 143            | 993               |

Question: If you capture network traffic, in which of the following protocols can you extract login credentials: SMTPS, POP3S, or IMAP?

> **Answer:** IMAP

***

## Task 5: SSH (Secure Shell)

SSH (Port 22) was created to replace the insecure Telnet (Port 23). It provides a secure, encrypted channel for command-line access. Beyond just "remote desktop for text," SSH supports:

* **Tunneling:** Routing other traffic through the encrypted SSH connection.
* **X11 Forwarding:** Running graphical applications remotely.

<figure><img src="../.gitbook/assets/image (74).png" alt="" width="563"><figcaption></figcaption></figure>

Question: What is the name of the open-source implementation of the SSH protocol?

> **Answer:** OpenSSH

***

## Task 6: SFTP and FTPS

While they sound similar, they use different security methods:

* **SFTP:** Uses the SSH protocol (Port 22).
* **FTPS:** Uses TLS encryption (similar to HTTPS) over standard FTP.

Question: Click on the View Site button to access the related site. Please follow the instructions on the site to obtain the flag.

> **Answer:** THM{Protocols\_secur3d}

***

## Task 7: VPN (Virtual Private Network)

A VPN creates a secure "tunnel" across an untrusted network (like the Internet). It encrypts all traffic between your device and the VPN server, hiding your true IP address and protecting your data from your Local ISP.

<figure><img src="../.gitbook/assets/image (73).png" alt="" width="563"><figcaption></figcaption></figure>

Question: What would you use to connect the various company sites so that users at a remote office can access resources located within the main branch?

> **Answer:** VPN

***

## Task 8: Closing Notes

To analyze encrypted traffic in tools like Wireshark, analysts often use "SSL Key Logging" to decrypt the session. Without the proper keys, the data remains undecipherable gibberish.

Question: One of the packets contains login credentials. What password did the user submit?

> **Answer:** THM{B8WM6P}

***

## Conclusion

This concludes the Networking module. We have traveled from the basic theory of the OSI model to the essential protocols like DHCP/ARP, into the core functionality of DNS/HTTP, and finally into the security frameworks that keep our data safe today. Understanding this progression is vital for both defending networks and identifying the weaknesses in legacy systems.
